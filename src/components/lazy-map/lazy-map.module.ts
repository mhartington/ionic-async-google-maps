import { NgModule } from '@angular/core';
import { LazyMapComponent } from './lazy-map.component'
@NgModule({
  declarations: [LazyMapComponent],
  exports: [LazyMapComponent]
})
export class LazyMapModule{}
